import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to="/" className="flex items-center h-full cursor-pointer">
      Ecommerce
    </Link>
  );
};

export default Logo;
